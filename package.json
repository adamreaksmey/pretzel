{
  "name": "presence-paas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "libs/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "publish:all": "npm publish -w @adamthedeveloper/pretzel-types --access public && npm publish -w @adamthedeveloper/pretzel-sdk --access public && npm publish -w @adamthedeveloper/pretzel-sdk-react --access public"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.9.0"
  }
}
