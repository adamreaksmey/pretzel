# @adamthedeveloper/pretzel-types

Shared TypeScript interfaces for Pretzel services and SDKs.

## Usage

```ts
import type {
  PresenceEvent,
  UserPresence,
} from "@adamthedeveloper/pretzel-types";
```
